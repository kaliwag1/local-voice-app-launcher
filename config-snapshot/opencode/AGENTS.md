# Jake's machine — standing instructions

- "Desktop" means `C:\Users\JakeW\OneDrive\Desktop`. That is the desktop Windows displays (OneDrive folder backup is on).
  `C:\Users\JakeW\Desktop` is a leftover local folder that is NOT visible to the user — never write there.
- Documents is the normal `C:\Users\JakeW\Documents` (not OneDrive).
- Everything runs locally (LM Studio for the model). There is no internet-dependent API to rely on for tasks.
- After creating or changing files, state the full path you used.
