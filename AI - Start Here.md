# AI assistants: start here

The project's shared context lives in the app repo:
`qwen-audio-agent-editable\docs\ai-context\README.md` (also pointed to by `CLAUDE.md` / `AGENTS.md` there).

This folder only holds the launcher/rebuild/push scripts and a snapshot of the user config.
